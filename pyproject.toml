[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "godand-bank"
version = "0.1.0"
description = "A banking backend built with FastAPI and PostgreSQL"
requires-python = ">=3.12"

[tool.setuptools.packages.find]
include = ["app*"]